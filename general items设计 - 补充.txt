general items
//绊雷：放置+触控爆炸	(参照绊线)
//遥控雷:丢出去+手动爆炸	(暂搁置)
//RPG:不列入考虑 武器	(暂搁置)
//手雷：丢出去定时爆炸	(7s=140tick(GameTick)=70tick(RedstoneTick))
//能够共同实现的就只有爆炸..
virtual class wpn_general_explosive extends item
{
	public:
		void Explode(int x,int y,int z);
	    float getExplosionRadius(); 		//获得爆炸半径 若有所区别则@Override 否则在此处固定定义 
	private:
		float explosiveRadius;
	
}

//有子弹射击的武器

virtual class wpn_general_bullet extends item
{
	public:
		int returnCapacity(){ //返回最大耐久度(子弹？热量？233)
			return this.maxDamage;
		}
		int shoot(general_bullet itemBullet,int x,y,z,vector dir); //射♂击一次/一颗子弹
		int setweaponMode(int par1); //设定射击模式
		int getWeaponMode();
	
		@Override
		public boolean onItemUse(ItemStack par1ItemStack, EntityPlayer par2EntityPlayer, World par3World, int par4, int par5, int par6, int par7, float par8, float par9, float par10); //使用时
		public ItemStack onItemRightClick(ItemStack par1ItemStack, World par2World, EntityPlayer par3EntityPlayer); //单击时
		public void onPlayerStoppedUsing(ItemStack par1ItemStack, World par2World, EntityPlayer par3EntityPlayer, int par4); //结束射击时
//注:自动武器(9mmAR,gauss左键,EGON 使用onItemUse/onPlayerStoppedUsing方法)，非自动武器(手枪，霰弹枪，RPG...)使用onItemRightClick方法
	private:
		int weaponMode; //武器模式
		int modeNum=1; //射击模式
}

